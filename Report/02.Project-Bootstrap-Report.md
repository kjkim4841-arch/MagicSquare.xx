# MagicSquare_xx — 프로젝트 부트스트랩 보고서

**작성일:** 2026-06-04  
**프로젝트:** MagicSquare_xx  
**세션:** 4 — ECB Harness · 명칭 정리 · Cursor Rules · 원격 동기화  
**상태:** Harness 골격 완료, 도메인 구현·테스트 본문 대기

---

## 1. 요약

Mom Test·문제 정의(`Report/01*`, `docs/PRD.md`) 이후, **ECB + Dual-Track TDD**용 최소 Harness를 루트에 구성하고, 잘못 쓰인 **1004** 명칭을 **xx**로 통일했다. AI 협업 규칙 초안(`.cursorrules`)을 추가했으며, 로컬 브랜치 3개를 GitHub 원격에 반영했다.

| 항목 | 결과 |
|------|------|
| Harness | `pyproject.toml`, `src/{entity,control,boundary}`, `tests/*` (빈 패키지) |
| 명칭 | `MagicSquare_1004` → `MagicSquare_xx`, `magic-square-xx` |
| Cursor Rules | `.cursorrules` 71줄 초안 |
| Git | `main` / `spec` / `staging` → `origin` 동기화 (`f5978f8`) |

---

## 2. 산출물

### 2.1 디렉터리 (Harness)

```
MagicSquare_xx/
├── .cursorrules
├── pyproject.toml          # pytest만, pythonpath=src
├── src/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
├── tests/
│   ├── entity/__init__.py
│   ├── control/__init__.py
│   └── boundary/__init__.py
├── docs/PRD.md
└── Report/01*, Report/02* (본 문서)
```

- 구현·테스트 **본문 없음** (의도적 — RED 단계 전 골격만).
- `Report/02`는 본 부트스트랩 세션 문서로 가정 (사전 존재 문서와 별도 번호).

### 2.2 `.cursorrules` 핵심 (초안)

| 영역 | 규칙 |
|------|------|
| 도메인 | 4×4, `0`×2, 1~16, 상수 34, 출력 `int[6]` 1-index |
| 오류 | E001~E007 → boundary; entity는 E001~E005 **금지** |
| ECB | boundary→control→entity, entity 상위 import 금지 |
| Dual-Track | Logic `D-*` / `test_d_*` (Domain Mock 금지), UI `U-*` / `test_u_*` (Mock 허용) |
| TDD | RED→GREEN→REFACTOR, skip/xfail/assert 완화 금지, MagicConstant SSOT |

### 2.3 명칭 변경 (5파일)

- `pyproject.toml`, `docs/PRD.md`, `Report/01.MagicSquare_ProblemDefinition_Report.md`, `Report/01.Mom-Test-STEP1-Report.md`, `Prompting/01.Mom-Test-STEP1-Transcript.md`
- CLI 모듈 예시: `python -m magicxx verify`
- 중복 표기 `MagicSquare_xx / MagicSquare_xx` → `MagicSquare_xx`로 정리

---

## 3. Git · 원격

| 브랜치 | HEAD | 원격 |
|--------|------|------|
| `main` | `f5978f8` | `origin/main` |
| `spec` | `f5978f8` | `origin/spec` |
| `staging` | `f5978f8` | `origin/staging` |

**커밋 이력 (본 세션 관련):**

| SHA | 메시지 |
|-----|--------|
| `d562b51` | Add ECB + Dual-Track TDD harness skeleton. |
| `f5978f8` | Rename MagicSquare_1004 identifiers to MagicSquare_xx. |

**원격:** https://github.com/kjkim4841-arch/MagicSquare.xx.git

---

## 4. 아키텍처 결정 (세션 4)

1. **ECB 3계층** — Rule→entity, Command/Skill→control, CLI/I/O→boundary (PRD 8계층 중 Rule·Command·Test Loop와 정렬).
2. **Dual-Track** — Logic(entity/control)은 도메인 Mock 없이 단위 검증; boundary만 I/O Mock 허용.
3. **pytest markers** — `entity`, `control`, `boundary` (Track·Layer 선언과 연동 예정).
4. **MagicConstant SSOT** — 구현 시 `34`/`16`/`4` 리터럴 산재 금지 (`.cursorrules`).

---

## 5. Mom Test · PRD 연결

| PRD/성공 기준 | Harness에서의 다음 단계 |
|---------------|---------------------------|
| S1 10조건 일괄 점검 | `tests/entity` — `D-*` RED |
| S2 실패 조건명·합 출력 | `tests/control` + boundary 포맷 |
| S3 대각선 NG 즉시 탐지 | 회귀 `test_d_*` 케이스 |
| FR-C1 `verify` CLI | `tests/boundary` — `U-*` |

---

## 6. 검증

```powershell
cd c:\DEV\MagicSquare_xx
python -m pip install pytest
python -m pytest
```

- 기대: `no tests collected` (골격만 존재).

---

## 7. 리스크 · 가정

| 리스크 | 대응 |
|--------|------|
| `.cursorrules`와 PRD 용어 불일치 (`verify` vs `int[6]` 출력) | entity RED 전에 PRD·rules 한 줄 정합 |
| E001~E007 코드표 미정의 | boundary RED 시 코드표 `docs/` 또는 Report에 고정 |
| 3브랜치 동일 SHA | 이후 `spec`=규격, `staging`=통합, `main`=릴리스 전략 확정 |

**가정:** `Report/02`는 본 부트스트랩 보고서; 별도 `Report/02` 원고가 있었다면 번호 충돌 시 병합 검토.

---

## 8. 다음 액션

- [ ] `entity` — MagicConstant SSOT + 격자 유효성 `D-*` RED
- [ ] `control` — 10조건 검증 오케스트레이션 `D-*`
- [ ] `boundary` — E001~E007·CLI `U-*` RED
- [ ] PRD FR-R6 빈칸 정책 (`skip` vs `incomplete`) Test Loop에서 확정
- [ ] `.cursorrules` 커밋 여부 사용자 결정 후 원격 반영

---

## 9. 참고 문서

- `Report/01.MagicSquare_ProblemDefinition_Report.md`
- `docs/PRD.md`
- `Prompting/02.Project-Bootstrap-Transcript.md`
- `.cursorrules`
