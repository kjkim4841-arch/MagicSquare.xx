# MagicSquare_xx — D-LOC-01 RED 세션 보고서

**작성일:** 2026-06-04  
**프로젝트:** MagicSquare_xx  
**세션:** 5 — entity Logic Track · D-LOC-01 RED 설계 · 스켈레톤 · 의도적 FAIL  
**상태:** RED 확인 완료 (`pytest.fail`), GREEN 대기 (`src/` 미구현)

---

## 1. 요약

Session 4 Harness 이후, **첫 Logic Track RED**로 빈칸 좌표 도메인(`FR-LOC-01` / `D-LOC-01`)을 착수했다. `/red-test-plan`으로 C2C·RED 설계표·테스트 플랜을 확정하고, `/red-skeleton`으로 `tests/`만 추가해 **의도적 실패**를 검증했다. `src/`는 변경하지 않았다.

| 항목 | 결과 |
|------|------|
| Phase / Layer / Track | RED · entity · Logic (`D-LOC-01`) |
| 설계 | C2C Rule 1~3, Track B 표, G1 fixture, D-LOC-01~03 범위(이번 구현은 01만) |
| 테스트 | `tests/conftest.py`, `tests/entity/test_d_loc_01.py` |
| pytest | `1 failed` — `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` |
| `src/` | 변경 없음 |

---

## 2. PRD · 요구 추적

### 2.1 FR-LOC-01 (PRD gap)

`docs/PRD.md` v0.1에는 **FR-LOC-01** 본문이 없다. 본 세션 RED는 아래를 **추적 초안**으로 사용했다.

| 출처 | 내용 |
|------|------|
| **FR-LOC-01 (초안)** | 4×4 격자에서 `0` 빈칸 위치를 **row-major** 순으로 나열, 좌표 **1-index** `(행, 열)` |
| **FR-R4** | 빈칸 `0`, 최대 2개 |
| **`.cursorrules`** | 솔버 `int[6]` 좌표 1-index (1~4) |

**판단 (To-Do T-LOC-01):** `blank_coords` 계열 API는 **유효 격자(0×2)** 만 입력. 빈칸 개수 오류·E002는 boundary / `D-GRID-INVALID-BLANKS-3`로 미룸. entity는 **E001~E005 emit 금지**.

### 2.2 D-LOC-01 Given / When / Then

| | 내용 |
|---|------|
| **Given** | **G1** 격자 — `0`×2, 1~16 각 최대 1회 |
| **When** | `find_blank_coords(grid)` *(GREEN 시 함수명 확정)* |
| **Then** | `[(2, 2), (3, 3)]` (1-index, row-major) |

**G1 데이터:**

```text
[[1,2,3,4], [5,0,7,8], [9,10,0,12], [13,14,15,16]]
```

---

## 3. RED 설계표 (Track B — 이번 묶음)

| Test ID | 대상 함수 (GREEN 예정) | Given→Then | Invariant | Expected RED |
|---------|------------------------|------------|-----------|----------------|
| **D-LOC-01** | `entity.loc.find_blank_coords` 등 | G1 → `[(2,2),(3,3)]` row-major | len=2; 1-index; Domain Mock 금지 | `pytest.fail` (스켈레톤) |
| D-LOC-02 | 동일 | G1 → 좌표 ∈ {1..GRID_SIZE} | `MagicConstant` SSOT | *(미작성)* |
| D-LOC-03 | 동일 | G1 → 길이 2·좌표 일치 | E00x 없음 | *(미작성)* |

---

## 4. 산출물 (tests/)

### 4.1 `tests/conftest.py`

- **`grid_g1`** fixture: G1 격자 복사본 반환 (스캔·도메인 로직 없음).
- `entity.constants` import는 **`src/entity/constants.py` 부재** + RED 단계 `src/` 수정 금지로 **보류** (주석으로 GREEN 시 복구 예정).

### 4.2 `tests/entity/test_d_loc_01.py`

- `@pytest.mark.entity`
- `test_d_loc_01_blank_coords_row_major(grid_g1)`
- AAA 주석 (Given / When / Then)
- **Then:** `pytest.fail("RED: D-LOC-01 — 구현 없음, 의도적 실패")` 한 줄만 (assert·skip·xfail 없음)

---

## 5. 검증

```powershell
cd c:\DEV\MagicSquare_xx
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
```

| 항목 | 값 |
|------|-----|
| 수집 | 1 item |
| 결과 | **FAILED** (의도적) |
| 메시지 | `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` |
| exit code | 1 |

**부수 시도:** conftest에 `from entity.constants import …` 적용 시 수집 전 `ModuleNotFoundError` — `entity/constants.py` 없음.

---

## 6. ECB · Mock · SSOT

| 규칙 | 준수 |
|------|------|
| Logic Track Domain Mock | ✅ `patch`/가짜 entity 없음 |
| entity E001~E005 | ✅ emit·assert 없음 |
| `src/` RED 금지 | ✅ entity 구현 없음 |
| MagicConstant SSOT | ⚠️ GREEN 전 `entity/constants.py` 추가 후 conftest import 권장 |
| skip / xfail | ✅ 미사용 |

---

## 7. 리스크 · 다음 액션

| 리스크 | 대응 |
|--------|------|
| PRD에 FR-LOC-01 미기재 | GREEN 전 `docs/PRD.md`에 FR-LOC-01 문장 추가 |
| 함수명 `find_blank_coords` vs `blank_coords_row_major` | GREEN 시 설계표와 단일 이름으로 통일 |
| D-LOC-02·03 미작성 | REFACTOR 전 또는 GREEN 직후 동일 파일에 RED 확장 |

**다음 Phase 제안:**

1. **GREEN** — `entity/constants.py` (SSOT) + `find_blank_coords` 최소 구현 → `pytest.fail` 제거, assert로 `[(2,2),(3,3)]` 검증  
2. **RED** — `D-LOC-02`, `D-LOC-03` 스켈레톤·assert 추가  
3. **PRD** — FR-LOC-01 정식 반영

---

## 8. 참고 문서

- `docs/PRD.md`, `docs/TDD-RED-Todo.md`
- `.cursor/skills/magic-square-tdd/SKILL.md`, `reference.md`
- `.cursor/commands/tdd-red.md`, `review-ecb.md`
- `Report/02.Project-Bootstrap-Report.md`
- `Prompting/03.D-LOC-01-RED-Transcript.md` (본 세션 Export)
